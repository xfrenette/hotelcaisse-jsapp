import Plugin from './Plugin';

/**
 * Sets an interval that calls the check() method on the
 * supplied Auth instance.
 */
class IntervalAuthCheck extends Plugin {
	constructor(auth) {

	}
}

export default IntervalAuthCheck;
